ext {
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
    // Required by @capacitor-firebase/authentication's Android Google
    // Sign-In support (see its docs/setup-google.md) -- without this the
    // plugin's native Google provider code isn't even compiled in, so
    // signInWithGoogle() fails immediately, before any Google account
    // picker ever opens (exactly what was reported).
    rgcfaIncludeGoogle = true
    androidxCredentialsVersion = '1.3.0'
}